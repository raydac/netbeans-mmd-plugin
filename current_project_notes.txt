## 1. Implementation Progress
- Version set to **1.7.0** for release (2026-09-06): root + all module `pom.xml`, IntelliJ `plugin.xml`, changelog/README headers, mac `Info.plist`, annotation-processor fallback.
- scia-reto: PlantUML dependency bumped to 1.2026.8 (`plantuml.version` in `scia-reto/pom.xml`). GitHub release exists; Maven Central not published yet — installed jar+sources into local `~/.m2` from the GitHub release so offline resolve works.
- scia-reto: assembly launch scripts fixed — Windows `%~dp0`+CRLF (+ nojre `start.cmd`), linux-nojre honors `JAVA_HOME`, quoted paths/`"$@"`, mac jar resolve + Apple `--add-opens`, AppImage `Exec=AppRun`.
- mind-map-model: API-stable correctness fixes — MindMap `readResolve` restores listeners after deserialize; Topic.parse honors `ignoreErrors` and skips snippet body when start missing; setExtra/removeExtras/removeAllExtras call `detachedToTopic`; moveToNewParent rejects descendant cycles; parent cleared on delete/remove; findPrev null checker matches findNext; MMapURI no longer shares mutable empty Properties; equals includes fileUriFlag; ModelUtils.toFile guards empty path; JavaLoggerServiceImpl no longer mutates JUL root level; MindMapModelEvent.getPath returns a clone; cloneTopicInMap checks map ownership.
- IJ sources use type imports plus `import static` for frequent factories (`getLogger`, `getBundle`, `getEncoder`/`getDecoder`, `UTF_8`, Swing/AWT constants) so method bodies do not use FQCN.
- IJ 1.6.10 hardening: single document parse path, file links open in the IDE, PSI/editor share module-aware root resolution, facet "inside browser" honored via HTMLEditorProvider with system-browser fallback, settings migrated from `application-components` to `applicationService`, config listener unregistered on editor dispose.
- IJ 1.6.10: MMD disappeared from File | New on latest IntelliJ because New File now reads declarative `com.intellij.fileType`, not deprecated `FileTypeFactory`.
- Replaced `fileTypeFactory` with `<fileType>` inside `<extensions>` (supported since 2019.2; project already requires 222.4554.10).
- Added `CreateMindMapFileAction` to `NewGroup` so File | New keeps an explicit SciaReto Mind Map item on both old and new IDEs.
- New-menu item anchored `after` `NewDir` (Directory), so it sits below File / Scratch File / Directory.
- Removed `MindMapFileTypeFactory`.
- Corrected file-template group name to `SciaReto Mind Map.mmd` (matches `fileTemplates/SciaReto Mind Map.mmd.ft`).
- Latest IDE `getInternalTemplate()` no longer fabricates a dummy template; if the name is missing it calls `getJ2eeTemplate()` and throws `Template not found`. Empty-map template is now also under `fileTemplates/internal/` and `fileTemplates/j2ee/`, registered as `<internalFileTemplate name="SciaReto Mind Map"/>`. Create action resolves across categories and writes default MMD text if none is found.
- mind-map-swing-panel: cleaned Swing action/event handling without API changes — title-editor key adapter held as field, dead typed Enter end-edit and unused `lastReleasedEvent` removed, duplicate drag hit-test removed, zoom keyboard path shared via `applyScaleAndRefresh`, popup state listener attached once per menu, `catchEscInParentDialog` wired once per dialog root pane, dead default `doActionForTopic` external branch removed.
- scia-reto UI Swing audit (module-only): PlantUML render no longer clears `lastSuccessfullyRenderedText` in finally before EDT success; loader animation unregister moved to EDT completion; `hideTextPanel` no longer re-adds KeyListener; DnDTree null-safe transferable; loading GIF redraw marshaled to EDT; project unload animation unregister on EDT.
- scia-reto: FlatLaf focus accent on large workspace scroll containers frozen via `UiUtils.disableContainerFocusBorder` (`focusWidth: 0` + `focusedBorderColor` = normal border); regular container borders remain.
- scia-reto: exit paths unified via `MainFrame.tryCloseApplication` / `shutdownApplication`; `TextFileBackup.finishAndAwait()` drains pending backup/delete jobs then joins the daemon worker so exit neither loses flush nor hangs the process.

## 2. Architecture & Technical Discoveries
- IJ: settings stay an `applicationService` (`MindMapApplicationSettings`); removed deprecated `preload="true"` so the marketplace verifier is satisfied — service still initializes lazily on first `getService`/`getInstance` (plugins registered in the constructor).
- Do not put `<fileType>` as a root `<idea-plugin>` child. 222 PluginManager treats that as `Unknown element: fileType` (`XmlReader.readRootElementChild`). It must live under `<extensions defaultExtensionNs="com.intellij">`.
- `fileType` attributes that exist on 222: `name`, `implementationClass`, `fieldName`, `language`, `extensions`. `name` must match `FileType.getName()`, `language` must match `MMLanguage` id `NBMindMap`.
- `MindMapFileType` constructor is private; `fieldName="INSTANCE"` is required.
- Latest IntelliJ New File popup lists file types from the declarative EP. `FileTypeFactory` is deprecated and ignored or unloaded there, so MMD vanished from New while the editor could still open existing files.
- `CreateFileFromTemplateAction` on 222: constructor `(text, description, icon)`, `buildDialog(Project, PsiDirectory, Builder)`, `getActionName(...)`. `createFile` resolves the template via `getInternalTemplate`, which also falls back to Files-category templates.
- Panel shortcuts are KeyListener + `MindMapPanelConfig` maps, not panel ActionMap/InputMap. Host IDEs must not assume Swing Action registration on the panel.
- Title editor Enter end-edit must use keyPressed/keyReleased (`getKeyCode()`); KEY_TYPED has `VK_UNDEFINED` so typed-path end-edit was dead.
- Shift+Enter next-line intentionally handled in both typed and released for RUS layout under Linux/IDEA.
- scia-reto: tree model notifications already use `Utils.safeSwingCall`; subtree load mutates `children` off EDT but listeners are notified on EDT via `notifyProjectStateChanged` / `invokeLater` fireNotifySubtreeChanged.
- scia-reto: `MindMapPanelConfig.getScaleModifiers()` still uses old `KeyEvent.CTRL_MASK` style — keep `getModifiers()` when matching wheel/scale shortcuts against that config.

## 3. Technical Debt & Refactoring
- Files-category template plus `NewGroup` action can show two "SciaReto Mind Map" entries on older File | New menus. Prefer keeping both over missing the item on the new New File popup.
- No unit tests under `idea-mindmap`.
- `IdeaUtils` still uses reflection for Maven, TransactionGuard, and HTMLEditorProvider to stay on 222.
- Plugin menu wiring (`AbstractExporter`/`AbstractImporter`/`ChangeColorPlugin`/`OptionsPlugin`) still duplicates ExternallyExecutedPlugin listener boilerplate; consolidate later via protected helper on `AbstractPopupMenuItem` without changing public plugin APIs.
- `Utils.isKeyStrokeEvent` is public and unused; keep for API stability (deprecate later if desired).
- scia-reto `DnDTree` drop onto folders still unimplemented (rejects / logs); TransferHandler.canImport always false.
- scia-reto scale gestures still tied to deprecated `getModifiers()` + `CTRL_MASK` until panel config migrates to `getModifiersEx()`.

## 4. Current Work In Progress
- Release 1.7.0 prepared (06-sep-2026): all pom.xml modules, idea-mindmap `plugin.xml`, README/changelog headers, mac Info.plist `CFBundleShortVersionString`, and AP fallback version set to `1.7.0` (no SNAPSHOT).
- Already aligned before this bump: `SciaRetoStarter.IDE_VERSION`, win-launcher `res/versioninfo.json`.
- Remaining assembly non-script hygiene (shared `<id>withjre_windows</id>`, `mac-nojre.xml` `icon.svgicns` typo, linux `fileMode`) still open if desired later.
- Next after tag/publish: bump to `1.7.1-SNAPSHOT` (or next planned) for continued development.
- Verify File | New create on latest IntelliJ after reinstall; New menu order after Directory.
- Verify opening an existing MMD file after the blank-editor fix.
- Verify FlatLaf editor/explorer containers keep normal border (no focus-accent color change).
- Verify scia-reto exits cleanly via window close, File | Exit, and platform Quit after editing text files.

## 5. Known Issues & Limitations
- Sandbox/build target remains `IC-222.4554.10`; latest-IDE New File UI cannot be verified in that sandbox.
- `MindMapPanel.getModel()` throws if no model has been set; IJ editor now installs a default empty map immediately after construction.
- `processPopUpForShortcut` with empty selection only selects root and does not open the menu; `VK_CONTEXT_MENU` always opens — intentional difference, leave unless UX change is requested.
- scia-reto assembly (non-script): all descriptors still share `<id>withjre_windows</id>`; `mac-nojre.xml` exclude typo `icon.svgicns`; linux fileSets omit explicit `fileMode` 0755; `mac-aarch64/` tree unused (packs from `mac/`).
- AppImage: Linux `Taskbar API is not supported` at start is expected and does not block the GUI; packaging (AppRun/desktop/JRE/`libawt_xawt`) is fine for GUI. Early console is quiet because JUL starts at WARNING until after splash. `StartupWMClass=scia.reto.editor` does not match real WM_CLASS `SciaReto`.

## 6. Open Questions & Pending Decisions
- Whether to hide the Files-category auto-template (move to `fileTemplates/internal/`) if older IDEs show a duplicate New item.

## 7. Useful References
- Register file type: https://plugins.jetbrains.com/docs/intellij/registering-file-type.html
- File templates / New menu: https://plugins.jetbrains.com/docs/intellij/using-file-templates.html
- Build plugin: `mvn22 -pl idea-mindmap -am package` from `mind-map/` (Gradle is invoked as `gradle8`).
- Swing panel compile: `mvn22 -pl mind-map-swing-panel -am compile` from `mind-map/`.
- scia-reto compile: `mvn22 -pl scia-reto -am compile` from `mind-map/`.

## 8. User Advice and Recommendations
- Keep File | New compatibility with latest IntelliJ and with previous versions back to 2022.2.5 (222.4554.10).
- Do not break public APIs of mind-map-swing-panel when cleaning internal Swing action handling.
- Do not break public APIs of mind-map-model (signatures, Serializable UIDs, logger SPI) when fixing internals.
- Restrict scia-reto Swing audits/fixes to the `scia-reto` submodule unless the user widens scope.
- For user-facing fixes and changes, update both root `README.md` (Changelog section) and `changelog.txt`.
